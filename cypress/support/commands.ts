/// <reference types="cypress" />

// Add custom commands here if needed
declare global {
  namespace Cypress {
    interface Chainable {
      // Add custom command types here if needed
    }
  }
}

export {}; 